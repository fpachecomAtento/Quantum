var mongoose = require('mongoose');
var user = mongoose.model('user');

module.exports = function(req, res){
	user.findById(req.params.id,function(err, resToGet){
		if(err) res.send(500, err.message);
		res.status(200).jsonp(resToGet);
	});
}
